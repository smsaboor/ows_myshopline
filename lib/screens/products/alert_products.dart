import 'package:flutter/material.dart';

class AlertDilogProducts extends StatelessWidget {
  const AlertDilogProducts({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
